export const initSupabase = () => {
  console.log("Supabase инициализирован (заглушка)");
};

export const fetchChats = async () => {
  console.log("Получение чатов из Supabase (заглушка)");
  return [];
};

export const saveChat = async (chat: any) => {
  console.log("Сохранение чата в Supabase (заглушка)", chat);
};
